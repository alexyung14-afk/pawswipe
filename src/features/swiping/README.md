# Swiping

The swipe interface: card stack, preference filters (including species: dog/cat/other), and the
likes screen. Reads animals through `src/shared/data` (the `AnimalRepository`) — never calls a
provider directly.

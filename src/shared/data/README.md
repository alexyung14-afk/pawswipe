# Data (AnimalRepository)

The `AnimalRepository` — one interface, many providers (RescueGroups.org now; ShelterLuv planned
as a V2 per-shelter pilot). Filters by species (dog/cat/other) as well as breed, size, age, location.
Adding or dropping a source touches only this folder, never the swiping/profile/applications code.

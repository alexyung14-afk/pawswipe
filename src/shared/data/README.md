# Data (DogRepository)

The `DogRepository` — one interface, many providers (Petfinder, RescueGroups, ShelterLuv).
Adding or dropping a source touches only this folder, never the swiping/profile/applications code.
